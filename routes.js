export const routes = {
  protected: ["/profile", "/dashboard", "/settings", "/appointments", "/legal-gpt"], // Pages that require authentication
  guest: ["/signin", "/signup"], // Pages for unauthenticated users
};
